# Python dependencies for RVC Voice Cloning Service

# Web Framework
Flask==3.0.0
Flask-CORS==4.0.0

# RVC Core Dependencies
# Note: RVC requires Python 3.9-3.11 (not 3.12+)

# Option 1: Use pre-built RVC package (easier)
# rvc-python==0.1.5  # Using latest available version

# Since we're using Hugging Face models instead of RVC, we can skip this

# Option 2: Install RVC dependencies manually (if Option 1 doesn't work)
# torch==2.1.0
# torchaudio==2.1.0
# torchvision==0.16.0
# fairseq==0.12.2
# pyworld==0.3.4
# librosa==0.10.1
# numpy==1.24.3
# scipy==1.11.4
# soundfile==0.12.1
# praat-parselmouth==0.4.3
# torchcrepe==0.0.21
# faiss-cpu==1.7.4
# ffmpeg-python==0.2.0

# Audio Processing
pydub==0.25.1
audioread==3.0.1

# Utilities
requests==2.31.0
python-dotenv==1.0.0
